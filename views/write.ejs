<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= category %> 게시판에 글쓰기</title>
  <style>
    body {
      font-family: sans-serif;
      padding: 20px;
      max-width: 600px;
      margin: auto;
    }

    textarea {
      width: 100%;
      box-sizing: border-box;
    }

    button {
      padding: 10px 20px;
      font-size: 16px;
    }

    @media (max-width: 768px) {
      body {
        padding: 10px;
      }

      button {
        width: 100%;
      }
    }
  </style>
</head>
<body>

  <h2><%= category %> 게시판에 글쓰기</h2>

  <form action="/write/<%= category %>" method="POST">
    <label>내용:<br/>
      <textarea name="content" rows="5" required></textarea>
    </label>
    <br/><br/>
    <button type="submit">등록</button>
  </form>

</body>
</html>
